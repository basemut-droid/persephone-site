# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# CMS OAuth broker secret (GitHub Client Secret) -- must NEVER be committed,
# this repo is public. Uploaded once by hand via FTP, not through the
# automated deploy. See HANDOFF.md's CMS-broker section for the full plan.
public/cms-secrets.local.php

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# Default save location some Claude interfaces write generated files to --
# not a project folder. Confirmed (2026-09-15) to hold only stale/duplicate
# copies of files that already exist properly in docs/ (some outdated, some
# byte-identical) plus screenshots not otherwise referenced -- never the
# source of truth for anything. Left in place on disk, not deleted outright
# (unlike a tracked file, there'd be no way back if that turned out wrong);
# just kept out of git going forward.
Claude outputs/
